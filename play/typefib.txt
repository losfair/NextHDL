fn entry() -> uint<32> {
  typefib<32, 30.cast(uint<32>)>();
}

fn typefib<W: uint<>, N: uint<W>>() -> uint<W>
  where N.eq(1).or(N.eq(2))
{
  1.cast(uint<W>);
}
  default
{
  typefib<W, N.sub(1)>().add(typefib<W, N.sub(2)>());
}
